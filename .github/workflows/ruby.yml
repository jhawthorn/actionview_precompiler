name: Test

on: [push]

jobs:
  ruby:
    runs-on: ubuntu-latest
    name: Ruby
    steps:
    - uses: actions/checkout@master
    - name: Run tests
      container: ruby
      run: |
        bundle install --jobs 4 --retry 3
        bundle exec rake
